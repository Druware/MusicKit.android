package com.druware.musickit

import android.app.Activity

/**
 * An empty activity for the instrumented tests to park the hidden page in.
 *
 * The page is a 1x1 child of an activity's content view and must stay attached to a window for its
 * renderer to live, so there is no way to exercise it without one.
 */
class MusicKitTestActivity : Activity()
